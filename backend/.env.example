# ============================================
# SUPABASE CONFIGURATION (Required)
# ============================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-or-service-key

# Storage bucket name (Supabase Storage)
STORAGE_BUCKET=documents

# ============================================
# AI/ML SERVICES (Required)
# ============================================
# OpenRouter API for GPT-4 classification
OPENROUTER_API_KEY=your-openrouter-api-key

# OpenAI API for embeddings
OPENAI_API_KEY=your-openai-api-key

# ============================================
# GMAIL INTEGRATION (Optional)
# ============================================
# Gmail credentials for email processing
# Place gmail_credentials.json in backend/ directory
# Run gmail_setup.py to authenticate

# ============================================
# SERVER CONFIGURATION
# ============================================
# Backend port (Render will override with $PORT)
PORT=8000

# ChromaDB configuration
CHROMA_HOST=localhost
CHROMA_PORT=8000

# ============================================
# PRODUCTION SETTINGS
# ============================================
# Environment (development/production)
ENVIRONMENT=production

# Allowed CORS origins (comma-separated)
CORS_ORIGINS=https://your-frontend.netlify.app,https://your-frontend.vercel.app

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
